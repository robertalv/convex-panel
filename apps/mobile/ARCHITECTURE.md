# 📱 Convex Panel Mobile - Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      CONVEX PANEL MOBILE                        │
│                     React Native + Expo                          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         APP ENTRY                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  App.tsx                                                   │  │
│  │  • QueryClientProvider (React Query)                      │  │
│  │  • ThemeProvider (Light/Dark mode)                        │  │
│  │  • AuthProvider (Session management)                      │  │
│  │  • DeploymentProvider (Current selection)                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                         NAVIGATION                               │
│                                                                  │
│  ┌───────────────────┐        ┌──────────────────────────────┐ │
│  │   Not Logged In   │        │        Logged In             │ │
│  ├───────────────────┤        ├──────────────────────────────┤ │
│  │ WelcomeScreen     │        │ ┌──────────────────────────┐ │ │
│  │ DeviceAuthScreen  │        │ │    Bottom Tabs           │ │ │
│  └───────────────────┘        │ ├──────────────────────────┤ │ │
│                               │ │ • Dashboard             │ │ │
│                               │ │ • Alerts                │ │ │
│                               │ │ • Projects              │ │ │
│                               │ │ • Settings              │ │ │
│                               │ └──────────────────────────┘ │ │
│                               └──────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                      DATA LAYER                                  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  React Query (Server State)                               │  │
│  │  • useTeams()                                             │  │
│  │  • useProjects()                                          │  │
│  │  • useDeployments()                                       │  │
│  │  • useDeploymentInsights()                                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                               ↓                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  BigBrain API Client                                       │  │
│  │  • getTeams()                                             │  │
│  │  • getProjects()                                          │  │
│  │  • getDeployments()                                       │  │
│  │  • rollbackDeployment()                                   │  │
│  │  • disableFunction()                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                             │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐ │
│  │ Convex BigBrain  │  │ Auth0 OAuth      │  │ Push Notifs  │ │
│  │ api.convex.dev   │  │ auth.convex.dev  │  │ Expo         │ │
│  └──────────────────┘  └──────────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     DEVICE STORAGE                               │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Secure Storage (iOS Keychain / Android Keystore)         │  │
│  │  • Access Token                                           │  │
│  │  • Refresh Token                                          │  │
│  │  • Session Data                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔐 Authentication Flow

```
1. User opens app
   ↓
2. WelcomeScreen shown (if not authenticated)
   ↓
3. User taps "Sign in with Convex"
   ↓
4. DeviceAuthScreen appears
   ↓
5. App requests device code from auth.convex.dev
   ↓
6. User sees code (e.g., ABCD-EFGH)
   ↓
7. User copies code and opens browser
   ↓
8. User pastes code and authorizes
   ↓
9. App polls for token (every 5 seconds)
   ↓
10. Token received!
   ↓
11. Exchange OIDC token for BigBrain access token
   ↓
12. Store token securely
   ↓
13. Navigate to MainTabs (Dashboard)
```

## 📊 Data Flow

```
Component
   ↓ (uses hook)
React Query Hook (e.g., useTeams)
   ↓ (fetches from)
API Client (bigbrain.ts)
   ↓ (HTTP request)
BigBrain API (api.convex.dev)
   ↓ (response)
React Query Cache
   ↓ (provides to)
Component (re-renders with data)
```

## 🎨 Theme System

```
ThemeContext
   ↓
Provides: { theme, isDark, toggleTheme }
   ↓
Components use: const { theme } = useTheme()
   ↓
Apply colors: style={{ color: theme.colors.text }}
   ↓
Auto-updates when theme changes
```

## 📱 Screen Hierarchy

```
RootNavigator
├── Auth Stack (not logged in)
│   ├── WelcomeScreen
│   └── DeviceAuthScreen
│
└── Main Tabs (logged in)
    ├── Dashboard Tab
    │   └── DashboardStack
    │       ├── DashboardOverview
    │       ├── FunctionDetails
    │       └── ErrorDetails
    │
    ├── Alerts Tab
    │   └── AlertsStack (TODO)
    │       ├── AlertsList
    │       ├── AlertDetail
    │       └── NotificationSettings
    │
    ├── Projects Tab
    │   └── ProjectsStack (TODO)
    │       ├── ProjectsList
    │       └── ProjectDetail
    │
    └── Settings Tab
        └── SettingsStack (TODO)
            ├── SettingsScreen
            └── ProfileScreen
```

## 🗂️ Code Organization

```
src/
│
├── api/                    # External API clients
│   └── bigbrain.ts        # Convex BigBrain API
│
├── components/            # Reusable UI components
│   ├── shared/           # (TODO) Buttons, Cards, etc.
│   └── layout/           # (TODO) Layouts, Containers
│
├── contexts/             # React Context providers
│   ├── AuthContext.tsx   # Authentication state
│   ├── ThemeContext.tsx  # Theme management
│   └── DeploymentContext.tsx  # Current deployment
│
├── features/             # Feature modules
│   ├── dashboard/        # (TODO) Dashboard feature
│   ├── alerts/           # (TODO) Alerts feature
│   └── settings/         # (TODO) Settings feature
│
├── hooks/                # Custom React hooks
│   └── useBigBrain.ts    # Data fetching hooks
│
├── navigation/           # Navigation configuration
│   ├── RootNavigator.tsx # Root navigation
│   ├── AuthStack.tsx     # Auth flow
│   ├── MainTabs.tsx      # Tab navigation
│   └── DashboardStack.tsx # Dashboard screens
│
├── screens/              # Screen components
│   └── auth/
│       ├── WelcomeScreen.tsx
│       └── DeviceAuthScreen.tsx
│
├── services/             # Core services
│   ├── auth.ts          # OAuth implementation
│   ├── storage.ts       # Secure storage
│   └── notifications.ts  # Push notifications
│
├── types/               # TypeScript definitions
│   └── index.ts         # All types
│
├── utils/               # Utility functions
│   ├── date.ts         # Date formatting
│   ├── error.ts        # Error handling
│   ├── format.ts       # Number formatting
│   └── validation.ts   # Input validation
│
└── App.tsx             # App entry point
```

## 🔄 State Management Strategy

```
┌──────────────────────────────────────────────────────┐
│                  SERVER STATE                         │
│            (React Query / TanStack Query)             │
│  • Teams, Projects, Deployments                       │
│  • Health metrics, Insights                           │
│  • Cached, auto-refreshed                            │
│  • Optimistic updates                                │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│                  GLOBAL UI STATE                      │
│                 (React Context)                       │
│  • Authentication (session, user)                     │
│  • Theme (light/dark mode)                           │
│  • Current deployment selection                       │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│                  LOCAL STATE                          │
│                  (useState)                           │
│  • Form inputs                                        │
│  • UI toggles (modals, accordions)                   │
│  • Component-specific data                           │
└──────────────────────────────────────────────────────┘
```

## 🚀 Build & Deploy Flow

```
Development
   ↓
Local Testing (Simulator/Emulator)
   ↓
Physical Device Testing (Expo Go)
   ↓
Internal Testing (TestFlight/Internal Testing)
   ↓
Beta Testing
   ↓
Production Build (EAS Build)
   ↓
App Store / Google Play Submission
   ↓
Production Release
```

## 📦 Dependency Layers

```
┌─────────────────────────────────────────┐
│          React & React Native            │
│        (Core Framework)                  │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│              Expo SDK                    │
│    (Device APIs, Build Tools)            │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│         Navigation & State               │
│  (React Navigation, React Query)         │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│            UI Libraries                  │
│    (Paper, Charts, Bottom Sheet)         │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│          App-Specific Code               │
│  (Screens, Components, Services)         │
└─────────────────────────────────────────┘
```

---

**This architecture provides:**
✅ Clean separation of concerns
✅ Scalable structure
✅ Type safety
✅ Easy testing
✅ Maintainability
